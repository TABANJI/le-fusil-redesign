# Browser-safe values supplied only when the future Supabase provider is enabled.
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Server-only. SUPABASE_SERVICE_ROLE_KEY must never be exposed to browser code.
SUPABASE_SERVICE_ROLE_KEY=

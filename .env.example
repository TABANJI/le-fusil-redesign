# Browser-safe values injected by the production build. These are not private secrets.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
PUBLIC_SITE_URL=
ADMIN_ALLOWED_EMAIL=

# Server/Edge Function secrets are configured directly in the trusted platform.
# Never place a service-role key, database password, JWT secret or SMTP password here.

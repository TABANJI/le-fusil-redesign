(function(g){'use strict';const config=g.LEFUSIL_SITE_CONFIG,key='lefusil_locale';function safeRead(){try{return localStorage.getItem(key)}catch{return null}}const requested=new URLSearchParams(location.search).get('lang'),browser=(navigator.language||'en').slice(0,2),locale=config.supportedLocales.includes(requested)?requested:config.supportedLocales.includes(safeRead())?safeRead():config.supportedLocales.includes(browser)?browser:config.defaultLocale;function set(next){if(!config.supportedLocales.includes(next))return;try{localStorage.setItem(key,next)}catch{}const url=new URL(location.href);url.searchParams.set('lang',next);location.href=url.href}g.LEFUSIL_LOCALE={current:locale,set};document.documentElement.lang=locale;document.documentElement.dir=locale==='ar'?'rtl':'ltr';})(window);

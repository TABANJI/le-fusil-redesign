(function(){const grid=document.querySelector('#featuredProducts');if(!grid)return;grid.innerHTML=LEFUSIL_PRODUCTS.filter(p=>p.featured).map(productCard).join('');syncShortlist()})();
function money(p){return p.price?new Intl.NumberFormat('en-US',{style:'currency',currency:p.currency,maximumFractionDigits:0}).format(p.price):'Price on request'}
function productCard(p){return `<article class="product-card reveal"><div class="product-image placeholder"><img src="${p.image}" alt="${p.brand} ${p.name}" onerror="this.hidden=true"><button class="shortlist" data-shortlist="${p.id}" aria-label="Add ${p.name} to shortlist">♡</button></div><div class="product-info"><div class="product-meta">${p.category} · ${p.brand}</div><h3>${p.name}</h3><div class="price">${money(p)}</div><div class="card-actions"><a class="btn btn-dark" href="product.html?slug=${p.slug}">View Details</a><button class="btn btn-outline" data-toast="Availability is confirmed personally by the store.">Request Availability</button></div></div></article>`}

# Appointment workflow

The form validates and stores a versioned local demo record, then mirrors a manual-compatible inquiry so existing Inquiries/CRM can reconcile normalized email, source `Appointment` and product interest. Dates are requests only, within 180 days, and require representative confirmation. Production requires server validation, rate limiting, Turnstile/CAPTCHA, abuse monitoring, durable storage and safe reference generation.

# Site configuration

Complete `js/site-config.js` only with verified store data: legal business name/registration, public email, phone, WhatsApp, address, map link, opening hours/timezone, social links, production domain, approved appointment limits, consent/privacy versions, actual analytics provider and PWA decision. Values marked `TO_BE_CONFIRMED` or `REPLACE_BEFORE_PRODUCTION` are intentionally not presented as real contact data; unavailable actions must remain hidden. Obtain owner and legal approval before production.
